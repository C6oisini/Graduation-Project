# 论文题目：面向端云协同拆分推理的多模态大模型语义感知度量隐私保护机制研究

**(Research on Semantic-Aware Metric Privacy Mechanisms for Multimodal Large Models in Split Inference)**

------

## 摘要 (Abstract)

**[背景与问题]**
随着以 Qwen-VL 为代表的多模态大模型（LVLMs）在移动端与边缘侧的应用需求激增，受限于端侧算力瓶颈，“端侧采集-云端推理”的**拆分学习（Split Learning）**架构成为主流部署方案。然而，该架构传输的中间层表征（Smash Data/Embeddings）面临着严峻的隐私挑战。攻击者可通过**嵌入层反演攻击（Embedding Inversion Attack）**，从高维特征向量中精确重构出用户的原始图像与文本输入。传统的差分隐私（DP）机制往往采用各向同性的噪声注入，忽略了多模态数据在高维语义流形中的结构特性，导致模型推理效用大幅下降。

**[方法]**
针对上述问题，本文提出了一种**面向多模态嵌入层的语义感知度量隐私保护框架**。
首先，基于拆分推理架构，在端侧编码器与云端大模型之间构建**语义隐私防火墙**。
其次，利用**度量隐私（Metric Privacy）**理论，在特征空间的单位超球面上设计了基于 **von Mises-Fisher (vMF)** 分布的概率性偏转机制。该机制在保留向量核心语义方向的同时，能够有效掩盖纹理、背景等敏感细节信息。 再次，针对文本与图像模态对噪声敏感度的差异，提出了**非对称隐私预算分配策略**，优先保障文本指令的语义一致性。
最后，基于 Qwen-VL-8B 模型实现了**非侵入式（Non-invasive）**的即插即用隐私层。

**[结果]**
实验结果表明，该机制在 MS-COCO 和 VQA-v2 任务上，能够将反演攻击的重构图像 SSIM 降低至 0.3 以下（隐私性），同时保持 85% 以上的图像描述生成质量（效用性），实现了优于传统方法的隐私-效用权衡。

**关键词：** 多模态大模型；拆分推理；度量隐私；语义感知；Qwen-VL；嵌入层反演攻击

------

## 第一章 绪论 (Introduction)

### 1.1 研究背景
### 1.2 问题陈述
### 1.3 本文研究目标与贡献

------

## 第二章 前置知识与问题定义 (Prerequisites and Problem Definition)

### 2.1 视觉语言模型 (VLM) 基础
- **Qwen-VL 架构解析：** 介绍视觉编码器（Visual Encoder）、对齐层（Projector）及语言模型（LLM）的构成。
- **Embedding 特征空间：** 阐述图像与文本如何在多模态大模型中被映射为高维 Embedding 向量。

### 2.2 隐私保护理论基础
- **差分隐私 (Differential Privacy):** 定义及核心机制（拉普拉斯/高斯机制）。
- **度量隐私 (Metric Privacy):** 基于度量空间的隐私定义（$d$-privacy）及其在连续表征保护中的优势。
- **von Mises-Fisher (vMF) 分布：** 介绍其作为单位超球面上正态分布替代方案的数学性质。

### 2.3 问题定义与威胁模型
- **拆分推理场景：** 定义端侧计算与云端计算的边界（Cut Layer）。
- **中间表征泄露：** 定义传输中的 Embedding 向量作为隐私泄露载体。
- **威胁模型：** 定义“诚实但好奇（Honest-but-curious）”的云端攻击者及攻击目标（特征反演、属性推理）。

------

## 第三章 相关工作 (Related Work)

### 3.1 拆分学习与推理隐私
### 3.2 差分隐私在深度学习中的应用
### 3.3 度量隐私与多模态数据保护

------

## 第四章 语义感知度量隐私机制设计 (Methodology)

### 4.1 语义度量空间的构建
- **流形假设：** 基于 LayerNorm 效应的超球面嵌入假设。
- **距离度量：** 测地线距离与余弦相似度的定义。

### 4.2 基于 vMF 的语义感知扰动算法
- **算法流程：** 分解、切平面投影、缩放、重投影。
- **理论分析：** 范数保持性证明、偏转角度与隐私预算 $\epsilon$ 的映射关系。

### 4.3 双模态非对称预算分配与掩码策略
- **分配逻辑：** 文本敏感度 vs. 视觉冗余度。
- **掩码层：** 系统指令的零扰动保护。

------

## 第五章 实验设计与分析 (Experiments)

### 5.1 实验设置与评价指标
- **基准模型与数据集：** Qwen-VL-8B, MS-COCO, VQA-v2。
- **指标定义：** 效用指标（CIDEr, Accuracy, CLIPScore）、隐私指标（ASR, SSIM）。

### 5.2 机制可视化与物理验证
- **几何分布：** 3D 球面扰动点簇分析。
- **数值稳定性：** 范数保持性（Norm Stability）对比实验。

### 5.3 隐私-效用权衡分析
- **核心对比：** vMF vs. Gaussian vs. Laplace 在全隐私预算下的表现。
- **定性分析：** 不同 $\epsilon$ 下的模型生成文本 Case Study。

### 5.4 消融实验与泛化性测试
- **消融分析：** 单模态 vs. 全模态保护。
- **泛化性：** 在 LLaVA 模型上的复现结果。

------

## 第六章 总结与展望 (Conclusion)

### 6.1 全文总结
### 6.2 局限性与展望

------

## 参考文献 (References)
